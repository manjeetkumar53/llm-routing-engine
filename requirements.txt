fastapi==0.116.1
uvicorn==0.35.0
pydantic==2.11.7
pytest==8.4.1
httpx==0.28.1
streamlit==1.46.0
plotly==6.1.1

# Optional: install only the provider(s) you need
# openai>=1.0          (for LLM_PROVIDER=openai)
# anthropic>=0.25      (for LLM_PROVIDER=anthropic)
# ollama uses httpx which is already installed above
